<section id="content">
    <div class="container">

        <div class="card">
            <div class="card-header ch-alt m-b-20">
                <h2>System Administrators
                    <small>Manage the system administrators that are granted permissions to configure all aspects of the system via the administration portal.</small>
                </h2>
                <ul class="actions">
                    <!-- <li>
                        <a href="">
                            <i class="zmdi zmdi-refresh-alt"></i>
                        </a>
                    </li>
                    <li>
                        <a href="">
                            <i class="zmdi zmdi-download"></i>
                        </a>
                    </li> -->
                </ul>
                <a href="<%= new_admin_path %>" class="btn bgm-red btn-float waves-effect"><i class="zmdi zmdi-plus"></i>
                </a>
            </div>

            <div class="table-responsive">
                <table id="grid-admins" class="table">
                    <thead>
                        <tr>
                            <th data-column-id="first_name">First Name</th>
                            <th data-column-id="last_name" data-order="asc">Last Name</th>
                            <th data-column-id="email">Email Address</th>
                        </tr>
                    </thead>
                </table>
            </div>

        </div>

    </div>
</section>

<%= content_for :body_end do %>
<%= init_grid(
  "grid-admins",
  url: "/admins.json",
  primaryKey: "admin_id",
  formatters: {

  },
  handlers: {
      click: { fn: :inspect, url: "/admins", identifier: 'admin_id' }
  }
) %>
<% end %>
