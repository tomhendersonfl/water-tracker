<% if current_user.notifications.unread.broadcast.count > 0 %>
<% @notification =  current_user.notifications.unread.broadcast.first %>
<section id="content">
  <div class="container ">
    <div class="row">
      <div class="alert alert-success alert-dismissible" role="alert">
        <%= form_for(@notification, url: notification_path(id: @notification.id), method: :patch) do |f| %>
        <%= @notification.body %>
        <%= f.button(type: "submit", class: "close") do %>
        <span aria-hidden="true">×</span>
        <% end %>
        <% end %>
      </div>
    </div>
  </div>
</section>
<% end %>
